language: cpp

matrix:
  include:
    - os: linux
      dist: trusty
      compiler: clang
      addons:
        apt:
          packages:
          - libboost-test-dev

before_script:
  - mkdir build 
  - cd build

script:
  - cmake -G "Unix Makefiles" -DCML_BUILD_TESTING:Bool=ON ..
